import React from 'react';

function Card({Data}) {
    return (
        <>
            {
                Data.map((d,i) => {
                    
                })
            }
        </>
    );
}

export default Card;